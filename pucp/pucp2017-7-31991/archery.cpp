#include <iostream>
#include <vector>
#include <queue>
#include <string>
#include <cmath>

using namespace std;

#define MAXN 105

int main() {
    int i, j, t, n;
    double x, y;
    
    cin >> t;
    while(t--) {
        cin >> n;
        for(i = 0; i < n; ++i) {
            cin >> 
        }
    }
    return 0;
}