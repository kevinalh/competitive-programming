#include <vector>
#include <iostream>
#include <map>
#include <cstdio>

using namespace std;

#define MAXN 30000

long long n, i;
long long lengths[MAXN];

int main() {
    long long n, ;
    scanf("%lld", &n);
    for(i = 0; i < n; ++i) {
        scanf("%lld", &lengths[i]);

    }
    return 0;
}
